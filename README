Aluno: Weslley Daltro de Castro Silva
Linguagem: Python 3

Informações importantes:

	-> Biblioteca sys utilizada para encerrar o programa ao digitar 'e' 	

	-> Caso o programa apresente algum erro de impressão é porque foi utilizado python 3 e o interpretador rodado foi python 2 ou vice-versa, 
		basta mudar o formato de print que foi alterado de uma versão para outra. Versão 
			print python 2: print ("Texto %d") % (variavel)
			print python 3: print ( "Texto %d" % (variavel) )

	-> Acabei por erro meu fazendo uma função para percorrer a tabela do texto antes de fazer a do padrão, li a especificação do trabalho errado, porém
		no programa acabei deixando as 2 funções, a tabela do texto e a tabela do padrão, como no trabalho só foi solicitado a tabela do padrão, deixei a do
		texto comentado, caso queira conferir é só descomentar e nas operações: 's', 'u', 'e', é só digitar 't'.
	
	-> Comentários presente no código para facilitar o entendimento do mesmo.

	-> Caso exista algum erro de execução/compilação, por favor passar e-mail para leydaltro@gmail.com

	